.DS_Store
*.log
node_modules/
dist/
build/
.env
.env.*
assets/*resume*.pdf
.review/
.shots/

# Cached Notion page dumps from `phokus.py pull`. Regenerable, and they hold the
# private editor URLs the briefs deliberately withhold from the site.
tools/campaigns/_raw/
